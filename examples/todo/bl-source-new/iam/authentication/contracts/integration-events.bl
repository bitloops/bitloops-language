Struct IntegrationUserChangedEmailSchemaV1 {
    string userEmail;
    string userId;
}

IntegrationEvent UserChangedEmailIntegrationEvent(event: TodoCompletedDomainEvent) {  
    'v1' : IntegrationUserChangedEmailSchemaV1 {
        const userChangedEmail = IntegrationUserChangedEmailSchemaV1({
            todoId: event.data.id.toString(),
            userId: event.data.email.email,
        });
        return userChangedEmail;
    }
}