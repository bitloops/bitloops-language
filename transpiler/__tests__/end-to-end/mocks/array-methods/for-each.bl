DomainService MarketingNotificationDomainService () {
  public getNotificationTemplateToBeSent(): (OK(void), Errors()) {
        const sampleArray = [];
        sampleArray.forEach((element) => {
          console.log(element);
        });
  }
}
