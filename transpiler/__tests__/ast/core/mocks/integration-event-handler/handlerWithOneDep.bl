IntegrationEventHandler MoneyDepositedIntegrationEventHandler(commandBus: ICommandBus) {
    
    eventVersion: 'v1';
    
    handle(event: [banking][banking]MoneyDepositedIntegrationEvent) {
  
      this.commandBus.send(command);
    }
}